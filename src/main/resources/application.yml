server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iot_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ll521521
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000ms
    mongodb:
      uri: mongodb://localhost:27017/iot_platform

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

iot:
  jwt:
    secret: iot-platform-secret-key-2024-must-be-at-least-256-bits-long!!
    expiration: 86400000

netty:
  tcp:
    port: 8888

knife4j:
  enable: true
  setting:
    language: zh_cn
